两个文件修改完成，验证无误。

**修改1** — `readerTemplates.ts`：
- 下载按钮从 iframe 下方的 `position:absolute` 浮动层移到 `.pdf-toolbar` 中
- 下载按钮样式改为与全屏按钮同系色（`rgba(0,85,150,0.1)` 背景 + `rgba(0,85,150,0.2)` 边框 + 6px 圆角）
- 下载按钮内联 SVG 下载图标
- 去除了 iframe 下方多余的包裹层
- 清掉了 `⛶全屏阅读` 后面的多余字符 `\uE1F0`

**修改2** — `clientScript.ts`：
- 展开时：`'Exit Fullscreen'` → `'退出全屏'`（第 654 行）
- 折叠时：`'Fullscreen'` → `'⛶全屏阅读'`（第 663 行）
