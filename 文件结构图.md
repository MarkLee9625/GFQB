# 工法情报编辑器 - 应用文件结构图

## 项目概述
这是一个 React + TypeScript 项目，用于工法情报编辑和管理。

## 目录结构树状图

```mermaid
graph TD
    ROOT[工法情报编辑器 - 副本] --> FILES[根目录文件]
    ROOT --> COMPONENTS[components/]
    ROOT --> HOOKS[hooks/]
    ROOT --> SERVICES[services/]
    ROOT --> PUBLIC[public/]
    ROOT --> SRC[src/]
    ROOT --> DIST_TEST[dist_test/]
    
    FILES --> App.tsx
    FILES --> index.html
    FILES --> index.tsx
    FILES --> package.json
    FILES --> tsconfig.json
    FILES --> vite.config.ts
    FILES --> types.ts
    FILES --> 更新说明.md
    
    COMPONENTS --> CategoryManagerModal.tsx
    COMPONENTS --> Editor.tsx
    COMPONENTS --> ErrorBoundary.tsx
    COMPONENTS --> ExportOptionsModal.tsx
    COMPONENTS --> Icons.tsx
    COMPONENTS --> KeyboardShortcutsHelpModal.tsx
    COMPONENTS --> LoadingOverlay.tsx
    COMPONENTS --> NavigationCapsule.tsx
    COMPONENTS --> PaperView.tsx
    COMPONENTS --> Sidebar.tsx
    COMPONENTS --> Toolbar.tsx
    
    HOOKS --> useBlobManager.ts
    HOOKS --> useInView.ts
    HOOKS --> useJournal.ts
    HOOKS --> useMemoryMonitor.ts
    
    SERVICES --> aiService.ts
    SERVICES --> db.ts
    SERVICES --> exportService.ts
    SERVICES --> pdfExtractor.ts
    SERVICES --> wechatImporter.ts
    
    PUBLIC --> pdf.min.mjs
    PUBLIC --> pdf.worker.min.mjs
    PUBLIC --> CMAPS[cmaps/]
    
    SRC --> index.css
    SRC --> pdf-worker.d.ts
    SRC --> SRC_COMPONENTS[components/]
    SRC --> SRC_SERVICES[services/]
    SRC --> SRC_TYPES[types/]
    
    DIST_TEST --> index.html
    DIST_TEST --> pdf.min.mjs
    DIST_TEST --> pdf.worker.min.mjs
    DIST_TEST --> ASSETS[assets/]
    DIST_TEST --> TEST_CMAPS[cmaps/]
    
    ASSETS --> index--bTS4S8X.css
    ASSETS --> index-BG8ukz-D.js
    ASSETS --> pdf.worker.min-CXgfMxHN.mjs
    
    TEST_CMAPS --> 78-EUC-H.bcmap
    TEST_CMAPS --> Adobe-CNS1-0.bcmap
    TEST_CMAPS --> UniGB-UCS2-H.bcmap
    TEST_CMAPS --> [...更多字体映射文件]
```

## 关键文件说明

### 配置文件
- `package.json` - 项目依赖和脚本配置
- `tsconfig.json` - TypeScript 配置
- `vite.config.ts` - Vite 构建工具配置
- `tailwind.config.js.bak` - Tailwind CSS 配置备份

### 入口文件
- `index.html` - 应用主 HTML 文件
- `index.tsx` - React 应用入口点
- `App.tsx` - 主应用组件

### 核心组件
- `components/Editor.tsx` - 编辑器组件
- `components/Sidebar.tsx` - 侧边栏组件
- `components/PaperView.tsx` - 论文视图组件
- `components/Toolbar.tsx` - 工具栏组件

### 业务逻辑
- `hooks/useJournal.ts` - 日志/日记相关逻辑
- `services/aiService.ts` - AI 服务集成
- `services/pdfExtractor.ts` - PDF 提取功能
- `services/exportService.ts` - 导出功能

### 类型定义
- `types.ts` - 全局类型定义
- `vite-env.d.ts` - Vite 环境类型

### 构建输出
- `dist_test/` - 测试构建目录
- `public/` - 公共资源目录

## 项目技术栈

- **前端框架**: React + TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **PDF 处理**: PDF.js (pdf.min.mjs)
- **状态管理**: React Hooks
- **数据存储**: 本地数据库 (db.ts)

## 主要功能模块

1. **编辑器模块** (Editor.tsx, Toolbar.tsx)
   - 文本编辑功能
   - 富文本编辑支持

2. **侧边栏模块** (Sidebar.tsx)
   - 导航功能
   - 分类管理

3. **论文视图模块** (PaperView.tsx)
   - PDF 显示
   - 论文信息查看

4. **AI 服务模块** (aiService.ts)
   - 智能提取功能
   - 文本分析

5. **导出模块** (exportService.ts)
   - 多种格式导出
   - 数据转换

6. **分类管理** (CategoryManagerModal.tsx)
   - 分类编辑
   - 标签管理

## 项目特点

1. **模块化设计** - 组件分离清晰，职责单一
2. **类型安全** - 全面的 TypeScript 类型定义
3. **现代化工具链** - Vite + Tailwind CSS 组合
4. **PDF 集成** - 内置 PDF 查看和提取功能
5. **AI 增强** - 集成 AI 服务提升编辑体验
6. **响应式设计** - 支持不同设备尺寸

## 开发相关文件

- `更新说明.md` - 项目更新日志
- `CHANGELOG.md` - 变更日志
- `README.md` - 项目说明文档
- `.gitignore` - Git 忽略配置
- `package-lock.json` - 依赖锁定文件

## 测试和调试文件

- `build_debug.txt` - 构建调试信息
- `build_detailed_error.txt` - 详细错误日志
- `error_log.txt` - 错误日志
- `test_abstract_extraction.js` - 摘要提取测试
- `test_pdf_extraction.js` - PDF 提取测试

这个文件结构图展示了项目的整体架构和关键文件，有助于理解项目的组织方式和各个模块的功能。
