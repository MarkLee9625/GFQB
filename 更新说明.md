# SWS 工法情报系统 - 更新说明与优化记录

## 核心更新摘要 (2025年12月)

### 1. 导出系统稳定性强化与性能深度优化
- **资源完全内聚化**: 实现了导出过程中的图片全自动 Base64 嵌入逻辑。导出时，系统会自动下载文章正文、封面、封底的所有远程图片并编码为 Data URL 嵌入 HTML。
- **并发下载控制**: 引入并发限制（Concurrency Limit），有效防止因瞬时请求过多被服务器屏蔽，显著提升导出成功率。
- **数据注入安全增强**: 将数据注入方案从 JSON 字符串升级为 **Base64 编码注入**，彻底解决了特殊字符引起的脚本语法错误。
- **数据强一致性**: 导出前强制执行挂写冲刷 (`flushSaves`)，确保最新的内存编辑内容 100% 同步至导出包。
- **导出精度**: 100% 还原封面/封底图片的缩放倍率 (`scale`) 及平移坐标 (`posX`, `posY`)。

### 2. AI 智能辅助与抓取精度提升
- **Gemini AI 总结**: 集成 `gemini-3-flash-preview` 模型，一键生成文章 Why & How 总结。 UI 优化：将摘要输入框移至正文标题下方。
- **微信抓取 V4**: 增加了 **END 标记自动探测**与**噪声剔除算法**，自动清理文章末尾的二维码与干扰组件；解决了微信 GIF 动图在代理模式下的跳帧与静止问题。
- **首行缩进 (Auto Indent)**: 升级为 DOM 级递归算法，支持跨标签嵌套、顶级文本节点的精准缩进，不再受 HTML 结构复杂度的影响。

### 3. 系统逻辑与交互优化
- **分类重命名同步**: 实现分类名称的“一键变更”，自动批量更新数据库中所有相关文章的分类索引。
- **导入去重算法**: 优化项目导入逻辑，引入 ID 指纹去重，彻底根治了重复点击导入导致的内容翻倍 Bug。
- **工具栏样式统合**: 全面移除功能按钮的多余图标，实现纯文本扁平化设计，确保工具栏在不同窗口尺寸下的整齐与一致性。

### 4. 编辑器实时排版调优
- **动态字号与行距**: 增加 12px-36px 字体大小及 1.0-3.0 倍行距调节滑块，状态实时持久化至数据库。
- **居中对齐逻辑改进**: 实现居中对齐按钮的“状态切换”逻辑，支持点击再次取消居中，恢复原有排版。

### 4. 存储层与性能优化 (IndexedDB V2)
- **原子化存储**: 废弃单体 JSON 存储模式，改为每篇文章独立记录存储 (`journal_store`)，实现增量更新，大幅降低读写负荷。
- **智能 Blob 管理器**: 引入 `useBlobManager` 机制，统一管理内存中的图片资源，防止内存泄漏。
- **内存实时监控**: 新增 `useMemoryMonitor` Hook，实时监控 JavaScript 堆内存使用情况（阈值 500MB）。

### 5. 抓取服务与 PDF 增强
- **智能图片代理**: 使用 Weserv/AllOrigins 双代理解决微信图片防盗链，增强 GIF 动图防丢帧处理。
- **PDF 智能提取**: 升级基于字体大小分析的标题提取算法，优化摘要范围定位逻辑。

### 4. 视觉体验升级 (2025年12月24日)
- **默认字体黑体化**: 为提升可读性，系统全站默认字体由“宋体”切换为“黑体”系列（优先匹配苹方、微软雅黑、SimHei）。
- **标题 SVG 同步**: 封面、封底及侧边栏标题 SVG 字体同步更新为黑体，确保品牌视觉一致性。
- **导出样式全量适配**: 离线导出阅读版及打印版样式全面适配黑体序列。

---

### v1.0.24 (2025-12-26)
- **AI 辅助开发优化计划 (核心架构重组)**:
    - **导出服务模块化**: 将 `exportService.ts` 拆分为 `src/services/export/` 目录下的 `assets.ts` (样式), `templates.ts` (模板), `index.ts` (逻辑)，消除了数千行的单体文件。
    - **PDF 解析策略模式**: 将 `pdfExtractor.ts` 重构为 `src/services/pdf/` 结构，通过 **策略模式** 将标题识别、摘要提取、关键词检索逻辑解耦到独立的策略模块中。
    - **通用工具类解耦**: 建立了 `src/utils/fileHelpers.ts`，统一了 Base64、图片压缩及 Blob 转换逻辑，删除了 `db.ts` 和 `hooks` 中的重复实现。
    - **AI 行为准则 (.cursorrules)**: 建立了项目级规范文件，固化了技术栈、架构模式及编码标准，确保 AI 协作的高效与规范。
- **阅读器一致性修复**:
    - 同步了封面/封底的黑体字族、背景渐变及编辑团队布局。
    - 强化了视频/音频块的原子化删除体验，修复了光标在异步上传时的定位偏移。

### v1.0.23 (2025-12-24)
- **PDF 关键词智能提取**:
    - **算法增强**: 全新升级的 PDF 文本分析引擎，能够精准识别文档中的“关键词”、“Key words”区域。
    - **智能去重**: 自动解析以逗号、分号或空格分隔的关键词列表，并与现有标签进行智能合并去重。
    - **自动录入**: 上传 PDF 论文时，系统自动提取关键词并填充至文章标签，无需人工干预。
- **标签系统升级**:
    - **人工录入增强**: 编辑器新增标签人工录入指引，支持空格/回车/逗号快捷输入，且失焦自动保存。
    - **数据修正**: 全面优化了标签的存储结构与导出渲染逻辑，修复了阅读版标签样式缺失的问题。
    - **正则优化**: **findKeywordsInText** 算法不再强依赖 "Introduction" 等结束词，大幅提升对多种排版格式的兼容性。
- **阅读版视觉修复**: 
    - 补全了离线 HTML 中缺失的标签胶囊样式，确保与编辑器视觉一致。
    - 标签列表对齐与间距微调。

### v1.0.22 (2025-12-24)
- **导航位置优化**: 将翻页键从右下角移至屏幕两侧垂直居中，采用半透明悬浮箭头设计，大幅提升操作醒目度且不遮挡正文。
- **默认字体黑体化**: 全模块默认字体由宋体改为黑体。
- **导出模板升级**: 全面优化 `exportService` 中的硬编码字体引用。
- **SVG 矢量标题优化**: 更新所有组件中的 SVG 标题字体定义。

### v1.0.21 (2025-12-23)
- **导入指纹去重**: 修复了导入工程文件时文章数量翻倍的 Bug，增加 ID 唯一性检验。
- **微信抓取 V4**: 引入 `END` 智能截断逻辑，自动移除底部广告、二维码及分享引导；优化 GIF 图片代理链路，确保持续动态播放。
- **分类批量同步**: 新增分类重命名联动功能，修改分类名称时，所有历史文章自动迁移。
- **缩进算法升级**: `handleAutoIndent` 现在可以处理更复杂的 DOM 嵌套，确保首行缩进 100% 生效。
- **导出一致性保障**: 在生成导出包前强制同步所有待保存数据，防止数据丢失。

### v1.0.20 (2025-12-22)
- **AI 智能总结核心上线**: 集成 Gemini AI 接口，支持对航海工程/智能制造领域文章进行深度总结。
- **摘要排版优化**: 将摘要输入框从右侧边栏移至正文标题下方，优化输入体验并确保导出的离线版视觉焦点集中。
- **一键缩进 (Powerful Indent)**: 修复了“缩进”功能针对不同嵌套深度（如内联 Span、顶级文本）的兼容性，确保点击必应，排版必稳。
- **排版控制面板**: 增加了字体大小 (12-36px) 与行间距 (1.0-3.0) 精度滑块，支持保存至本地数据库，实现“文章级个性化排版”。
- **居中逻辑切换**: 修正了中心对齐按钮只能开启不能关闭的逻辑，现在具备 Toggle 态。
- **微信进口代理增强**: 引入 AllOrigins 代理，确保存储在微信服务器的 GIF 动图在第三方查看时不会出现加载失败。

### v1.0.19 (2025-12-22)
- **导出 PDF 阅读器修复**: 修复了导出阅读版中 PDF 阅读器窗口过小以及全屏功能失效的问题。
- **强健的全屏模式**: 采用了 DOM 重挂载策略实现全屏模式，彻底绕过堆叠上下文（Stacking Context）的限制，确保 PDF 阅读器能够完整覆盖全屏。
- **样式强制**: 为 PDF 阅读器添加了全局强制样式，确保高度和圆角在各种查看模式下的一致性渲染。

### v1.0.18 (2025-12-18)
- **导出系统强化**: 实现图片全量内聚、Base64安全数据注入。
- **性能重构**: IndexedDB V2 原子化存储，提升大数据量下的响应速度。
- **微信采集**: 深度优化，解决防盗链与干扰内容清洗。

### v1.0.17 (2025-12-14)
- **PDF 摘要提取优化**: 修复摘要重复、优化关键词识别、支持多种摘要格式。
- **PDF.js 稳定性修复**: 将动态脚本加载改为具名导入，解决全局变量 `pdfjsLib` 丢失问题。

### v1.0.16 (2025-12-13)
- **视觉统一**: 将阅读版与编辑版的所有“工法情报”标题统一为 SVG 矢量图及品牌蓝色渐变。
- **内存监控**: 新增 `useMemoryMonitor` Hook，支持 Chrome 内存实时监控。

### v1.0.15 (2025-12-13)
- **组件架构拆分**: 将 `App.tsx` 拆分为 `Sidebar`、`Toolbar`、`NavigationCapsule` 等独立组件。
- **键盘快捷键**: 实现全局快捷键系统（如 `Ctrl+N` 新建、`Ctrl+S` 保存），支持 `Ctrl+/` 查看帮助。
- **类型安全**: 完善 `types.ts` 定义，引入 `ErrorBoundary` 错误边界保护。
- **存储优化**: 数据库连接状态管理增强，实现批量保存与防抖机制。
- **Blob 内存管理**: 引入 `useBlobManager` 钩子，优化大型媒体文件的内存占用。
- **构建优化**: 配置 Vite 代码分割（vendor/editor/services），提升加载速度。

### v1.0.14 - v1.0.10 (2025-12-12)
- **封面样式修复**: 阅读版封面字体、排版与编辑版完美同步。
- **封底信息修复**: 恢复缺失的编辑/校对/审核人员信息。
- **视频排版修复**: 解决视频宽度溢出与段落缩进冲突。
- **阅读版控制栏**: 增加悬浮控制栏，支持目录切换与全屏。
- **GIF 动图支持**: 自动检测跳过压缩，保证 GIF 动画流畅播放。
- **反馈优化**: 增加大文件处理时的“Processing”全屏加载动画。
- **内核升级**: 预览与导出全面转向 Blob URL 渲染，大幅降低低配机器崩溃风险。

### v1.0.9 - v1.0.1 (历史阶段)
- **汉化修正**: 阅读版元数据标签全面转换为中文。
- **PDF 卡片美化**: 采用高级灰柔和风格，自动隐藏原文件名。
- **全新单文件导出**: 离线版升级为单文件离线 Web App，支持侧边栏导航与翻页。
- **架构升级**: 提取统一排版样式常量 `UNIFIED_STYLES`。
- **排版系统**: 引入 `.sws-prose` 标准，强制统一宋体 18px、2倍行高。
- **布局修复**: 解决了封面/封底上传按钮丢失、正文滚动背景截断、工具栏自动消失等布局缺陷。

### v1.0.0 (基准版本)
- **核心功能**: 基于 React + TS + Tailwind 重构，支持 IndexedDB 存储、图片拖拽、全屏阅读及离线包导出。

---

## 详细技术优化文档

### AI 智能总结与高效排版系统
**背景**: 随着文章内容量增加，提取核心 Why & How 成为刚需；同时，快速实现标准公文排版（如首行缩进）能显著提升效率。
**方案**:
- **Gemini 多模态接入**: 使用 `google-generative-ai` SDK，通过特定 Prompt 引导模型提取“技术必要性”与“实施收益”。
- **强健缩进算法**: 弃用简单的正则替换，采用原生 `NodeIterator` 递归遍历编辑器 DOM 树，确保在包含 `span`、`b` 等复杂行内标签的情况下依然能精准定位首行并添加缩进。
- **排版参数持久化**: 实现了一个基于 `article_store` 的排版上下文系统，确保字号、行距等偏好随文章存储，满足个性化排版需求。

### IndexedDB 存储层原子化重构
**背景**: 原单体 JSON 对象读写存在性能瓶颈。
**方案**:
- 将 `journal_store` 的 KeyPath 设为 `id`，实现文章级增量保存。
- 引入排序持久化 `articleOrder` 确保拖拽顺序在原子存储下依然生效。
- 500ms 防抖保存策略，平衡实时性与 I/O 开销。

### Editor UI 左右分栏工作台
**优化**:
- 由“垂直表单流”重构为“左右分栏模式”。
- **导航体验优化**：将翻页导航键从屏幕两侧移至文章内容底部，改为内联卡片设计，包含上下篇文章标题预览，增强引导性且不遮挡正文。
- **离线版字体对齐**：彻底修正导出 HTML 中的残留宋体引用，确保离线阅读版与编辑器风格统一（黑体系）。
- **右下角快捷键帮助**：保留并优化了右下角悬浮的键盘快捷键帮助图标。
- **左侧**: 专注内容编辑，采用文档级大标题输入框，工具栏轻量化。
- **右侧**: 固化元数据栏，独立滚动，提升垂直空间利用率。
- **效果**: 编辑区域从局促的弹窗感转变为专业工作台感。

### PDF 标题智能提取 (最大字号算法)
**逻辑**:
1. 加载首页并获取所有 TextItems。
2. 扫描得出最大字号 `maxFontSize`。
3. 筛选字号在 `maxFontSize * 0.9` 以上的文本。
4. 按坐标 Y 降序（从上到下）拼接。
5. 提供元数据读取失败后的终极兜底方案。

---

## 最新优化摘要（基于代码审查，2025-12-23）

### 导出系统深度优化
- **并发下载控制**: 引入并发限制（CONCURRENCY_LIMIT = 3），避免瞬时请求过多导致服务器屏蔽，显著提升导出成功率。
- **内存保护机制**: 单个文件超过20MB自动跳过Base64转换，防止大文件导致内存溢出。
- **资源内聚化**: 导出时自动下载远程图片并编码为Data URL，确保离线包完整。
- **Base64安全数据注入**: 将数据注入方案从JSON字符串升级为Base64编码，彻底解决特殊字符引起的脚本语法错误。
- **缩放与位置同步**: 封面/封底图片的缩放倍率（scale）及平移坐标（posX, posY）100%还原至导出包。

### 编辑器功能增强
- **AI标题生成**: 新增独立“AI拟题”按钮，基于文章内容生成精准标题。
- **智能首行缩进**: 采用DOM级递归算法，支持跨标签嵌套、顶级文本节点的精准缩进，不受HTML结构复杂度影响。
- **居中切换逻辑**: 居中对齐按钮具备Toggle状态，支持点击再次取消居中，恢复原有排版。
- **排版控制面板**: 增加字体大小（12-36px）与行间距（1.0-3.0）精度滑块，状态实时持久化至数据库。

### 系统稳定性提升
- **Blob URL内存管理**: 引入 `useBlobManager` 钩子，统一管理内存中的图片资源，防止内存泄漏。
- **临时Blob URL自动清理**: 导出生成的Blob URL在5分钟后自动清理，避免内存累积。
- **错误边界保护**: 实现 `ErrorBoundary` 和 `DBErrorBoundary` 组件，提供优雅的错误降级和用户友好提示。
- **键盘快捷键系统**: 全局快捷键支持（Ctrl+N新建、Ctrl+S保存、Ctrl+E导出、Ctrl+F全屏等），提升操作效率。

### 分类管理优化
- **分类重命名同步**: 修改分类名称时，自动批量更新数据库中所有相关文章的分类索引。
- **导入去重算法**: 优化项目导入逻辑，引入ID指纹去重，彻底根治重复点击导入导致的内容翻倍Bug。

### 微信抓取增强
- **END标记自动探测**: 自动清理文章末尾的二维码与干扰组件。
- **GIF动图代理增强**: 使用Weserv/AllOrigins双代理解决微信图片防盗链，确保GIF动图持续动态播放。

### 代码架构改进
- **组件模块化**: 将大型 `App.tsx` 拆分为多个独立组件（Sidebar、Toolbar、NavigationCapsule等），提高可维护性。
- **类型安全增强**: 完善 `types.ts` 定义，新增 `ExportOptions`、`KeyboardShortcut` 等接口，提升代码健壮性。
- **构建优化**: 配置Vite代码分割（vendor/editor/services），提升应用加载速度。

### 视频插入交互优化与段落间距调整
- **视频/音频原子化插入**: 将视频和音频插入逻辑修改为包裹在 `contenteditable="false"` 的容器中，确保Backspace键可以一次性删除整个媒体块，光标可以轻松定位到媒体前后。
- **段落间距调整**: 将段落（P）和媒体元素的垂直间距从28px缩小至16px，使页面更加紧凑。

### 光标位置修复与删除体验优化
- **选区保存与恢复**: 引入 `lastRangeRef` 存储光标位置，实现 `saveSelection` 和 `restoreSelection` 函数，确保异步文件上传时光标不会跳转到开头。
- **插入逻辑优化**: 在 `insertHtml` 函数中调用 `saveSelection` 和 `restoreSelection`，保持插入后光标在正确位置。
- **事件监听增强**: 在编辑器内容区域添加 `onMouseUp` 和 `onKeyUp` 事件监听，实时保存选区状态。

### v1.1.0 (2026-01-12)
- **核心架构升级 (The Componentization Era)**:
    - **UI 逻辑剥离**: 完成了 `App.tsx` 的精简化，将复杂的 UI 布局逻辑剥离至 `src/components/Layout/MainLayout.tsx`，实现了逻辑与展示的彻底分离。
    - **规范化组件导入**: 全面对齐 `.cursorrules` 规范，整合了 `hooks/useJournal.ts` 作为数据流的唯一真理源，消除了状态漂移风险。
- **视觉排版与设计升级**:
    - **杂志风设计 (Magazine Style)**: 封面与封底新增“杂志风格”设计模式，支持在编辑界面实时一键切换。
    - **导出增强**: 导出阅读版时自动携带所选设计风格，确保离线版与编辑预览的高度一致。
    - **导出交互优化**: 引入了 `ExportOptionsModal` 导出选项模态框，支持用户自定义导出参数（如是否包含图片、优化打印等）。
- **PDF 处理稳定性强化**:
    - **WASM 加速机制**: 修复了 PDF 解析在部分环境（尤其是离线或复杂安全域）下 WASM 加载失败的问题，通过显式指定 `wasmUrl` 提升了解码稳定性。
    - **分页流式插入**: 优化了 PDF 转图片插入正文的流程，采用分页挂载模式，解决了超长 PDF 转换导致内存溢出的风险。
    - **并发下载控制**: 优化了导出时的资源抓取逻辑，确保并发请求在各种网络环境下都能稳定完成。
- **交互体验调优**:
    - **全屏退出功能增强**: 优化了全屏模式下的工具栏逻辑，确保用户即便在全屏状态下也能轻松找到退出或切换功能。
    - **侧边栏快捷切换**: 工具栏新增侧边栏一键收纳按钮，最大化编辑视野。

### v1.1.1 (2026-01-12) - 构建架构重构
- **构建系统重构 (Build System V2)**:
    - **单次构建流程**: 废弃了不稳定的多重构建方案，采用单次标准构建 + 后处理注入 (`Post-Build Injection`) 策略，彻底解决了内存与 Buffer 溢出导致的构建崩溃问题。
    - **后处理注入引擎 (`scripts/post-build.js`)**:
        - **自动内联**: 智能识别并内联 JS/CSS 资源，生成单文件 HTML。
        - **PDF.js 离线注入**: 自动提取并注入 PDF.js 核心库与 Worker，通过 Blob URL 技术实现 100% 离线 PDF 渲染。
        - **自我复制**: 将构建产物作为模板注入回主应用，实现了编辑器与阅读器的"量子纠缠"——编辑器导出的阅读器即为编辑器自身的离线副本。
    - **环境隔离**: 创建了 `src/services/pdf/wrapper.ts` 隔离层，防止构建工具错误打包 PDF.js 巨型库文件。
    - **配置简化**: 移除了复杂的 `vite-plugin-singlefile` 插件依赖，回归 Vite 标准相对路径构建 (`base: './'`)。

最后更新：2026-01-12 11:10
