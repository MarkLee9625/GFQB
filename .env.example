# SWS 工法情报系统 - 环境变量模板
# ⚠️  安全警告：此文件仅包含占位符，实际敏感值请保存到 .env.local 文件中
# ⚠️  .env.local 文件已通过 .gitignore 配置，确保不会提交到版本控制系统

# AI 服务配置
# 从 Google AI Studio (https://makersuite.google.com/app/apikey) 获取 API Key
# ⚠️  安全警告：这是一个后端代理专用的密钥，绝不能以 VITE_ 开头，防止被前端构建工具意外暴露
GEMINI_API_KEY=your_gemini_api_key_here_do_not_commit

# 注意：
# 1. API Key 作为后端环境变量使用，不应暴露给前端代码
# 2. 实际开发时创建 .env.local 文件，并将真实值填入
# 3. 生产部署时，请使用服务器环境变量或安全的密钥管理系统
